// Self-service registration has been removed — only the Super Admin can create accounts
// via User Management. This file is intentionally unused (kept out of the route tree by
// register/page.tsx redirecting to /login).
export default function RegisterForm() {
  return null;
}
