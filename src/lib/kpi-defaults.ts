export const DEFAULT_FILL_WEIGHT_MG = 450;
export const DEFAULT_CAPSULES_PER_BOTTLE = 31;

/** Theoretical capsule output per hour for each 1Hz of encapsulation machine speed (measured: 40Hz -> 120,000 capsules/hour). */
export const CAPSULES_PER_HOUR_PER_HZ = 3000;
